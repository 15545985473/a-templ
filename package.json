{
  "name": "{{name}}",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{author}}",
  "license": "ISC",
  "bin": {
    "slp": "index.js"
  },
  "dependencies": {
    "commander": "^5.0.0",
    "download-git-repo": "^3.0.2"
  },
  "devDependencies": {},
  "description": "{{description}}"
}
